# CareerForge Environment Configuration
# This application is designed to be fully self-contained and run 100% locally.
# It does NOT use any cloud AI APIs or services like Gemini or OpenAI.
# NO API KEYS OR INJECTED SECRETS ARE REQUIRED.
# Everything is managed locally inside the application runtime.
